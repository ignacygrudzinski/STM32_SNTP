#include "blink.h"

#include "main.h"
#include <string.h>

// todo: do not use lwip
#include "lwip.h"
#include "lwip/inet.h"
#include "lwip/netdb.h"
#include "lwip/api.h"
#include "lwip/ip_addr.h"
#include <stm32f4xx_hal_gpio.h>
#include <stm32f4xx_hal_rtc.h>


// 39345550 - stack overflow question XD todo: implement

void BL_TaskFunc(void* param){
        // uint8_t second = 20;
        for(;;){
        //         RTC_TimeTypeDef time;
        //         RTC_DateTypeDef date;
        //         HAL_RTC_GetTime(&hrtc, &time, RTC_FORMAT_BIN);
        //         HAL_RTC_GetDate(&hrtc, &date, RTC_FORMAT_BIN);
        //         if (second != time.Seconds) {
        //                 second = time.Seconds;
        //                 blinkDiode();
        //         } else {
        //                 // printf("%d %d\r\n", second, time.Seconds);
        //         }
        //         // printf("%d-%d-%d\r\n", date.Year, date.Month, date.Date);
        //         // printf("%d-%d-%d-%d\r\n", time.Hours, time.Minutes, time.Seconds, time.SubSeconds);
                osDelay(100);
        }
}